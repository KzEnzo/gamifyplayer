{
  "name": "gamifyplayer",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "preview:fresh": "astro build && astro preview",
    "build:search": "astro build && pagefind --site dist --output-path dist/pagefind"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.3.0",
    "astro": "^5.7.0"
  },
  "devDependencies": {
    "pagefind": "^1.3.0",
    "typescript": "^5.8.0"
  }
}
